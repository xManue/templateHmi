# Diagnostic Menu Popup

Template collegato alla voce `Diagnostic` di `operator-shell`.

Espone le scelte `Diagnostic Zone` e `Diagnostic Device` tramite la callback
`onSelect`. Le destinazioni restano intenzionalmente fittizie finché non
verranno creati i relativi template di pagina.
