# Operator Shell Manuals Menu

Template collegato alla voce `Manuals` di `operator-shell`.

Le cinque voci del riferimento sono configurate in `src/templateData.js`. Il componente emette la voce selezionata tramite `onSelect`; le destinazioni verranno collegate quando saranno disponibili i rispettivi template manuale.
